kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: temporal-local
nodes:
  - role: control-plane
  - role: worker    # orchestrator pod 1
  - role: worker    # orchestrator pod 2 (topology spread requires 2 nodes)
